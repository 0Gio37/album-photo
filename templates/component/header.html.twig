<nav class="grid grid-cols-4 lg:grid lg:grid-cols-6 lg:justify-between lg:pt-1 p-2 mt-0 h-auto fixed w-full z-20 top-0 border-b border-gray-600 shadow-lg flex justify-between">

    <div class="flex justify-center hover:border-gray-500">
        <a class="flex text-white no-underline hover:text-white hover:no-underline" href="{{ path('home') }}">
            <div class="flex border border-yellow-400">
                <img class="w-12 transform duration-300 hover:scale-110" src="{{ asset('img/logo.png') }}" alt="logo" />
            </div>
        </a>
    </div>


    {# responsive menu : burger #}
    <div class="flex col-span-4 block lg:hidden flex justify-center col-span-4">
        <button>
            <svg id="menuNav" class="w-10 h-10 text-white" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                <path fill-rule="evenodd" d="M3 5a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zM3 10a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zM3 15a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1z" clip-rule="evenodd"></path>
            </svg>
        </button>

        {# responsive menu : block list #}
        <div id="menuList" class="absolute top-0 z-10 rounded-lg right-0 w-full m-auto hidden p-12 bg-gray-900">
            <div class="flex justify-end mr-10">
                <button id="closeBurgerList" class="p-3 text-2xl text-red-700 hover:text-red-300 flex justify-end transform transition duration-100 rounded-full border border-white">X</button>
            </div>
            <ul class="text-center md:flex-none justify-items-center px-5">
                <li class="pb-8">
                    <a class="inline-block py-2 px-4 text-lg text-white hover:text-blue-500 hover:underline transform transition duration-500" href="{{ path('displayAllTheme') }}">THEMES</a>
                </li>
                <li class="pb-8">
                    <a class="inline-block py-2 px-4 text-lg text-white hover:text-blue-500 transform transition duration-500" href="{{ path('displayAllYear') }}">ANNEES</a>
                </li>
                <li class="pb-8">
                    <a class="inline-block py-2 px-4 text-lg text-white hover:text-blue-500 transform transition duration-500" href="{{ path('displayAllAlbumAlpha') }}">ALBUMS</a>
                </li>
                <li class="pb-8">
                    <a class="inline-block py-2 px-4 text-lg text-white hover:text-blue-500 transform transition duration-500" href="{{ path('displayAllPhotoAntiChrono') }}">PHOTOS</a>
                </li>
            </ul>
        </div>
    </div>

    <div class="hidden lg:block flex col-span-4 m-auto">
        <ul class="list-reset flex m-auto md:flex-none items-center">
            <li class="mx-10">
                <a class="inline-block py-2 px-4  text-lg text-white hover:text-blue-500 rounded-lg hover:bg-gray-700 hover:scale-105 transform transition duration-500" href="{{ path('displayAllTheme') }}">THEMES</a>
            </li>
            <li class="mx-10">
                <a class="inline-block py-2 px-4 text-lg  text-white hover:text-blue-500 rounded-lg hover:bg-gray-700 hover:scale-105 transform transition duration-500" href="{{ path('displayAllYear') }}">ANNEES</a>
            </li>
            <li class="mx-10">
                <a class="inline-block py-2 px-4 text-lg  text-white hover:text-blue-500 rounded-lg hover:bg-gray-700 hover:scale-105 transform transition duration-500" href="{{ path('displayAllAlbumAlpha') }}">ALBUMS</a>
            </li>
            <li class="mx-10">
                <a class="inline-block py-2 px-4 text-lg  text-white hover:text-blue-500 rounded-lg hover:bg-gray-700 hover:scale-105 transform transition duration-500" href="{{ path('displayAllPhotoAntiChrono') }}">PHOTOS</a>
            </li>
        </ul>
    </div>

    <div class="col-start-6 flex justify-end px-8">
        <div class="relative inline-block">
            <button onclick="toggleDD('myDropdown')"  class="drop-button text-white text-sm focus:outline-none">
                <span class="pr-2"><i class="far fa-user-circle"></i></span>
                    Bonjour, {{ app.user.prenom }}
                <span class="text-white pl-2"><i class="fas fa-chevron-down"></i></span>
            </button>
            <div id="myDropdown" class="dropdownlist w-full absolute text-white right-0 overflow-auto z-30 invisible" style="background-color: #161616">
                {% if is_granted('ROLE_ADMIN') %}
                    <div class="mt-3 rounded-full bg-gray-700 hover:bg-blue-600">
                        <a href="{{ path('admin') }}" class="p-2 text-white text-xs no-underline hover:no-underline block"><i class="fas fa-user-shield"></i>  Mon DashBoard</a>
                    </div>
                {% endif %}
                <div class="mt-3 rounded-full bg-gray-700 hover:bg-blue-600">
                    <a href="{{ path('logout') }}" class="p-2 text-red-500 text-xs no-underline hover:no-underline block"><i class="fas fa-sign-out-alt fa-fw"></i>  Se déconnecter</a>
                </div>
            </div>
        </div>
    </div>

</nav>