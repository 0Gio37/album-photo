{% if is_granted('ROLE_ADMIN') %}
    <section class="w-3/5 pt-32 lg:pt-4 lg:w-full main-input bg-gray-800 pt-8" xmlns="http://www.w3.org/1999/html">
        <div class="lg:rounded-lg lg:bg-gradient-to-b from-gray-700 to-gray-500 p-8 lg:mx-60 lg:shadow-lg">
            <a href="{{ path('add_photo') }}">
                <div class="rounded-full py-4 bg-blue-900 hover:bg-blue-700 text-gray-400 px-8">
                    Ajouter des photos ...
                </div>
            </a>
        </div>
    </section>
{% endif %}