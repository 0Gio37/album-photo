{% extends 'base.html.twig' %}

{% block title %}Ajouter une photo{% endblock %}

{% block body %}

    <section class="bg-gray-800 pt-6">
        <div class="rounded-lg bg-gradient-to-b from-gray-700 to-gray-500 p-8 mx-60 shadow-lg">
            <div class="rounded-full py-4 bg-blue-900 text-gray-400 px-8">
                Formulaire d'ajout d'une nouvelle photo
            </div>
    </div>
    </section>

    <section>
        {{ form_start(photoForm) }}
        {{ form_row(photoForm.album) }}
        {{ form_end(photoForm) }}
    </section>

{% endblock %}
