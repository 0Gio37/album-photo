<div class="py-10">
    <section class="bg-gray-700 rounded-lg">
        {% for personn in taggedPersonns %}
            {% for item in personn %}
                <p class="text-white text-center lg:text-lg text-sm py-2 my-4">- {{ item.prenom}}  {{ item.nom }} - </p>
            {% endfor %}
        {% endfor %}
    </section>
</div>


